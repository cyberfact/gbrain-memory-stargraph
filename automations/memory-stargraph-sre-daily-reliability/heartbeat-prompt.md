Act as the Memory Stargraph SRE and run `mode=daily_reliability` in this persistent task. Read `automations/memory-stargraph-sre/prompt.md` completely. First enforce the quiet-time gate through the canonical prompt. When quiet, use the offline recurring-worker bridge evidence job, reason only over the local evidence bundle, submit a local decision/persist bundle, and report the reliability Run, incidents, capacity headroom, verification, TODO decisions, and approvals. Do not use task-local network, direct GBrain, PostgreSQL, approval fallback, or remediation unless separately authorized by the incident contract.
